<UserControl x:Class="WeScale.UserContr.SubUsCo_AfegirCarta.AfegirPersonatge"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <Border Background="#2E2E2E"
            CornerRadius="10"
            Padding="25"
            Margin="10">

        <StackPanel>

            <!-- CHECKBOX -->
            <Border Background="#3A3A3A"
                    CornerRadius="6"
                    Padding="10"
                    Margin="0 0 0 15">

                <CheckBox Content="Seleccionable en batalla"
                          Foreground="White"
                          FontSize="14"/>
            </Border>

            <!-- BOTON INVENTARIO -->
            <Button Content="📦 Gestionar Inventari"
                    Width="180"
                    Height="35"
                    Background="#444"
                    Foreground="White"
                    BorderBrush="#777"
                    BorderThickness="1"
                    Cursor="Hand"
                    HorizontalAlignment="Left">

                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#444"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#5A5A5A"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>

            </Button>

        </StackPanel>

    </Border>

</UserControl>